<!-- Material -->
<resources>
    <!-- lefomat theme inherits from the material theme -->
    <style name="lefomat" parent="android:Theme.Material.Light">
        <!-- theme customizations -->
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/accent</item>
        <!--  Text Color  -->
        <item name="android:textColorPrimary">@color/texticons</item>
        <!--  Background Window Color  -->
        <item name="android:windowBackground">@color/background_color</item>
        <!-- Bottom Navigationbar color -->
        <item name="android:navigationBarColor">@color/primary_text</item>

        <item name="dropDownListViewStyle">@style/PopupMenuListView</item>

        <item name="android:popupMenuStyle">@style/PopupMenuListView</item>

        

    </style>

    <!-- OverFlow menu Styles -->
    <style name="PopupMenuListView" parent="android:Widget.Material.Light.ListView.DropDown">
        <item name="android:divider">#FF0000</item>
        <item name="android:dividerHeight">2dp</item>
        <item name="android:background">#33B5E5</item>
        <item name="android:popupBackground">#33B5E5</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
    </style>

</resources>